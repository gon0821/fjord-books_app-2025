<% title = t('views.common.title_edit', name: Comment.model_name.human.downcase) %>
<% content_for :title, title %>

<h1><%= title %></h1>

<%= render 'form', commentable: @comment.commentable, comment: @comment %>

<nav class="page-nav">
  <%= link_to t('views.common.back', name: Comment.model_name.human), @comment.commentable %>
</nav>
