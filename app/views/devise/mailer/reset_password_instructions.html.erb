<p>こんにちは <%= @resource.email %>!</p>

<p>パスワード変更のリクエストが送信されました。以下のリンクから変更できます。</p>

<p><%= link_to 'パスワードを変更する', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>このメールをリクエストしていない場合は、無視してください。</p>
<p>上記のリンクにアクセスして新しいパスワードを作成するまで、現在のパスワードは変更されません。</p>
